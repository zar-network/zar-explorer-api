const {
  db,
} = require('../helpers');

const addresses = {

  getAddress(req, res, next) {
    //get from db
    //enrich with data from node? balances or my sync process needs to keep track of the balance. Not that bad.
  }
}

module.exports = addresses
